# 初探stream API
## 什么是stream API
`stream API`是一种用于处理数据流的API，它允许开发者以一种声明式的方式处理数据流，而无需关心底层实现细节。stream API的核心思想是将数据流视为一个连续的序列，每个元素都是一个事件，开发者可以通过操作这些事件来实现对数据流的处理。stream API的设计目标是提供一种简单、灵活、可组合的方式来处理数据流，使得开发者可以更加轻松地处理异步操作、错误处理、背压等问题。